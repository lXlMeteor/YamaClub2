[functions]
  # 関数の最大実行時間を26秒に設定（最大値）
  timeout = 26
  
[build]
  command = "npx prisma generate && next build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# 必要に応じて以下の設定も追加できます
[build.environment]
  NEXT_USE_NETLIFY_EDGE = "true"